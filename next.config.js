
// const withSass = require('@zeit/next-sass')
// module.exports = withSass({
// })

module.exports = {
    env: {
        api: 'http://localhost:3000',
    },
    images: {
        domains: ['localhost'],
    },
}